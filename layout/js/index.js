$(function() {
  //Клик на кнопку Войти в хедере
  $('#login_btn').on('click', function() {
    //сюда вставить анимацию формы входа
    $('.color_overlay').css('opacity', 1)
    $('.color_overlay').css('opacity', 1) //Клик через колор оверлей
    $('.color_overlay').css('opacity', 1) //Блюр заднего фона / Скейл заднего фона
    $('.color_overlay').css('opacity', 1) //Движение окна вправо

  });
});
